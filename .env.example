# Database Configuration
# ----------------------
# By default, the app uses a local SQLite database (no setup required).
# If you want to use PostgreSQL, uncomment the line below and set your credentials.

# DATABASE_URL=postgresql://postgres:password@localhost/virtual_fashion

# Fallback Settings
# -----------------
# USE_CLOUD=false  # Set to true if you want to use cloud APIs for heavy AI tasks
